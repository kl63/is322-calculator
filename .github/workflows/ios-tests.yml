name: iOS Unit and UI Tests

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: macos-14  # Ensure latest macOS version is used

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Select Xcode Version (15.4)
        run: sudo xcode-select -switch /Applications/Xcode_15.4.app

      - name: Install Dependencies
        run: |
          sudo xcode-select --switch /Applications/Xcode_15.4.app/Contents/Developer
          xcodebuild -version

      - name: List Available iOS Simulators
        run: xcrun simctl list devices

      - name: Build the App
        run: |
          xcodebuild clean build -project CalculatorApp.xcodeproj \
            -scheme CalculatorApp \
            -destination "platform=iOS Simulator,name=iPhone 15 Pro,OS=17.0"

      - name: Run Unit Tests
        run: |
          xcodebuild test -project CalculatorApp.xcodeproj \
            -scheme CalculatorApp \
            -destination "platform=iOS Simulator,name=iPhone 15 Pro,OS=17.0"

      - name: Run UI Tests
        run: |
          xcodebuild test -project CalculatorApp.xcodeproj \
            -scheme CalculatorAppUITests \
            -destination "platform=iOS Simulator,name=iPhone 15 Pro,OS=17.0"
